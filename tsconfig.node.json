{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2023"],
    "types": ["node"],
    "outDir": "./out/types/node",
    "rootDir": ".",
    "baseUrl": ".",
    "paths": {
      "@shared/*": ["src/shared/*"],
      "@domain/*": ["src/domain/*"],
      "@database/*": ["src/database/*"],
      "@collector/*": ["src/collector/*"],
      "@main/*": ["src/main/*"]
    }
  },
  "include": [
    "src/main/**/*.ts",
    "src/preload/**/*.ts",
    // The two utility-process entry points. scripts/build-workers.mjs bundles
    // these into the packaged app, so they are shipped code; leaving them out
    // of every project meant tsc never saw them and eslint could not parse
    // them, which made a green typecheck partly vacuous.
    "src/workers/**/*.ts",
    "src/collector/**/*.ts",
    "src/database/**/*.ts",
    "src/domain/**/*.ts",
    "src/shared/**/*.ts",
    "tests/unit/**/*.ts",
    "tests/integration/**/*.ts",
    "tests/nodeps/**/*.ts",
    "tests/support/**/*.ts",
    "tests/fixtures/**/*.ts",
    // format.ts is renderer code, but tests/nodeps/format.test.ts imports it:
    // it is a pure module with no DOM, and the honesty rules it enforces are
    // worth testing without a browser.
    "src/renderer/src/format.ts",
    // Imported with a JSON import attribute by src/main/index.ts.
    "resources/update-keys/keys.json",
    "scripts/**/*.mts",
    "electron.vite.config.ts",
    "vitest.config.ts",
    "playwright.config.ts"
  ]
}
